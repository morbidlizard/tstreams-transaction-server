package transactionService.impl.db

object TransactionTable {
  val stream = "stream"
  val partition  = "partition"
  val interval = "interval"
  val transactionId = "txn"
  val state = "state"
  val quantity = "qty"
  val timestamp = "timestamp"
}
